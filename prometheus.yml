global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'home-control-center'
    metrics_path: '/api/metrics'
    authorization:
      type: Bearer
      credentials: 'local-worker-secret-here'
    static_configs:
      - targets: ['app:3000']
