global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'home-control-center'
    metrics_path: '/api/metrics'
    authorization:
      type: Bearer
      credentials: '${WORKER_API_SECRET}'
    static_configs:
      - targets: ['app:3000']